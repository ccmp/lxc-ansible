- hosts: "{{ service_name }}-all"
  gather_facts: False
  remote_user: root
  vars_files:
    - ../{{ service_name }}/config/config.yml
  vars:
    pcon: "{{ container[cname] }}"

  roles:
    - { role: destroy , site: "{{ pcon }}"  }
