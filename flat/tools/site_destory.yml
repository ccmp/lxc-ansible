- hosts: "{{ service_name }}-all"
  gather_facts: False
  remote_user: root
  vars_files:
    - ../{{ service_name }}/vars/config.yml
  vars:
    pcon: "{{ container[contents[service_name]] }}"

  roles:
    - { role: destroy , site: "{{ pcon }}"  }
