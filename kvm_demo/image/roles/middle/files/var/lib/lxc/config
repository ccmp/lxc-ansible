lxc.network.type = veth
lxc.network.flags = up
lxc.network.link = {{ mnghost.lxc_default.iface }}
lxc.network.ipv4 = {{ mnghost.lxc_default.ip }}
lxc.network.ipv4.gateway = {{ mnghost.lxc_default.gateway }}

lxc.rootfs = {{ middle_container_dir }}/rootfs

# Common configuration
lxc.include = /usr/share/lxc/config/debian.common.conf

# Container specific configuration
lxc.mount = {{ middle_container_dir }}/fstab
lxc.utsname = {{ middle_container_name }}
lxc.arch = amd64
lxc.autodev = 1
lxc.kmsg = 0

